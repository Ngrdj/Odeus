import { RaceEnum } from "../race.enum";

export class CreateRaceDto {

    name:RaceEnum;
    strBonus:number;
    dexBonus:number;
    intBonus:number;
    constBonus:number;
    wisBonus:number;
    chaBonus:number;


}